apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: cass-lifecycle-dc1-0b60c5c2-default-sts
spec:
  template:
    spec:
      (containers[?name == 'cassandra'] | [0].resources.limits.memory): 2Gi
(status.observedGeneration == metadata.generation): true
status:
  readyReplicas: 3
  updatedReplicas: 3
  (currentRevision == updateRevision): true
